Juypter-aws is "Jupyter on AWS with batteries included - in 60 seconds".

This repository contains a CloudFormation template to spin up a Jupyter environment on AWS. CUDA support is bundled in if using the g2 or p2 instance family. A number of Jupyter notebooks are also supplied as examples (see /labs). All examples are provided as interactive Jupyter notebooks.

Enjoy. Please log many feature requests and bug reports!
